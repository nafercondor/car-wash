const self = (settings) => {
  const mongo = require("./src/mongodb");
  if (settings) {
    let newSettings = JSON.parse(JSON.stringify(settings));
    mongo._setSettings(newSettings);
  }
  return mongo;
};

module.exports = self;
